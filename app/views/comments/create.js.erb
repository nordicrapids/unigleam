$(".comment_question_div_<%= @comment.commentable.id %>").html("<%= j render(:partial => 'shared/survey_question_list', :locals => { :survey_question => SurveyQuestion.where(id:@comment.commentable.id).first  } ) %>")
$('.text_area_comment').val('')
